<?php if ($page == "recipeDetail") { $source = "recipe"; $dbTable = "recipes"; } else { $source = "default"; $dbTable = "default"; } if ($action == "scale") $scale = "Y"; else $scale = "N"; if ($amt == "default") $amt = 0; else $amt = $amt; echo output_print_recipe($row_log['id'], $source, $dbTable, "recipePrint", $row_log['brewStyle'], $scale, $amt, $row_log['brewName'], $imageSrc, $row_colorChoose['themeName']); ?>